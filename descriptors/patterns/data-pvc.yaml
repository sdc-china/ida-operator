apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ida-data-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: <STORAGE_CLASS_NAME>
  resources:
    requests:
      storage: 5Gi
